workshop struct Player {
    workshop func setAbility1Enabled(enabled: bool)
}

workshop struct Game {
    condition func inProgress(inProgress: bool)
}

struct Normal {
    func sayHello()
}

const hello = 10

rule "Rule Name" OngoingPlayer(Team2, Slot6) {
    condition game.inProgress()

    player.setAbility1Enabled(true)
    killAllPlayers()
}
