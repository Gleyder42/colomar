pub native enum Team {
    Team1, Team2, All
}

pub native enum Slot {
    Slot0, Slot1, All
}

pub native event OngoingEachPlayer(team: Team = Team.All, heroSlot: Hero | Slot = Slot.All) {
    native val player: Player;
}

pub native event OngoingGlobal() {

}