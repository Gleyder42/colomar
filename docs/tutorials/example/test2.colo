struct MyStruct {

}

enum MyEnum {
    Hello = 1,
    World = 2
}

enum MyVariant {
    Hello(string)
    World(Player, string)
}

fn test(my: MyVariant) {
   match (my) {
        Hello(string) => log(string),
        World(player, string) => player.smallMessage(string)
   }
}